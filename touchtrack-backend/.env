# ============================================
# TOUCHTRACK BACKEND ENVIRONMENT VARIABLES
# ============================================

# -----------------------------
# DATABASE CONFIGURATION
# -----------------------------

# Local Development Database URL
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE_NAME
# For Mac with default PostgreSQL: postgresql://YOUR_USERNAME@localhost:5432/touchtrack
# For Windows: postgresql://postgres:YOUR_PASSWORD@localhost:5432/touchtrack
DATABASE_USER=postgres
DATABASE_PASSWORD=user123
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=touchtrack
NODE_ENV=development
DATABASE_URL=postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}
# Production Database URL (from Render PostgreSQL)
# You'll get this after creating a database on Render
# Example: postgresql://touchtrack_user:xxxxxxxxxxxx@dpg-xxxxxxx.oregon-postgres.render.com/touchtrack_db
# DATABASE_URL=your_render_database_url_here

# -----------------------------
# SERVER CONFIGURATION
# -----------------------------

# Port the server will run on
PORT=5000

# Node environment (development, production, test)
NODE_ENV=development

# -----------------------------
# CORS CONFIGURATION (Optional)
# -----------------------------

# Allowed origins for CORS (comma-separated)
# In development, you might want: http://localhost:5173,http://localhost:3000
# In production, set this to your frontend URL
# ALLOWED_ORIGINS=http://localhost:5173

# -----------------------------
# SECURITY (Optional - for future enhancements)
# -----------------------------

# JWT Secret (if you add authentication later)
# JWT_SECRET=your_super_secret_key_here

# API Rate Limiting (requests per minute)
# RATE_LIMIT=100

# -----------------------------
# LOGGING (Optional)
# -----------------------------

# Log level (error, warn, info, debug)
# LOG_LEVEL=info

# -----------------------------
# NOTES FOR DEPLOYMENT
# -----------------------------

# When deploying to Render:
# 1. Don't commit this .env file to Git (it's in .gitignore)
# 2. Set environment variables directly in Render dashboard
# 3. Render will provide DATABASE_URL automatically when you create a PostgreSQL database
# 4. Set NODE_ENV=production in Render
# 5. PORT is set automatically by Render, but you can override it

# -----------------------------
# QUICK SETUP GUIDE
# -----------------------------

# 1. Copy this file and rename it to .env in your backend root
# 2. Replace YOUR_USERNAME with your PostgreSQL username
# 3. If you have a password, add it: postgresql://user:password@localhost:5432/touchtrack
# 4. Create the database: createdb touchtrack
# 5. Run seed script: npm run seed
# 6. Start server: npm run dev